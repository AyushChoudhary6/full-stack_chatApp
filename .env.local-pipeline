# =============================================================================
# Environment Variables for Local GitLab CI/CD Pipeline
# =============================================================================
# Updated for PostgreSQL database, no Cloudinary, local deployment only

# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================
DATABASE_URL=postgresql://chatapp_user:your_password@localhost:5432/chatapp
POSTGRES_PASSWORD=your_secure_postgres_password

# =============================================================================
# APPLICATION SECRETS
# =============================================================================
JWT_SECRET=your_super_secret_jwt_key_minimum_32_characters_long

# =============================================================================
# OPTIONAL GITLAB CI/CD VARIABLES (for local deployment)
# =============================================================================
# Only needed if you want to use a specific PostgreSQL setup
POSTGRES_DB=chatapp
POSTGRES_USER=chatapp_user

# =============================================================================
# REMOVED VARIABLES (no longer needed)
# =============================================================================
# CI_DOCKER_USER - Not needed for local deployment
# CI_DOCKER_PASSWORD - Not needed for local deployment  
# KUBE_SERVER - Not needed for local deployment
# KUBE_NAMESPACE - Not needed for local deployment
# KUBE_CA_PEM - Not needed for local deployment
# KUBE_TOKEN - Not needed for local deployment
# CLOUDINARY_CLOUD_NAME - Removed (not using Cloudinary)
# CLOUDINARY_API_KEY - Removed (not using Cloudinary)
# CLOUDINARY_API_SECRET - Removed (not using Cloudinary)
# MONGODB_URI - Replaced with DATABASE_URL for PostgreSQL
# DOMAIN_NAME - Not needed for local deployment

# =============================================================================
# GitLab Variable Configuration for Local Setup
# =============================================================================
# You only need to add these 2 variables in GitLab CI/CD Variables:
#
# 1. DATABASE_URL (masked)
#    Value: postgresql://chatapp_user:password@postgres:5432/chatapp
#
# 2. JWT_SECRET (masked)  
#    Value: your_32_character_secret_key
#
# 3. POSTGRES_PASSWORD (masked)
#    Value: your_secure_postgres_password

# =============================================================================
# Local Development Setup
# =============================================================================
# For local development, create a .env file in your backend directory with:
# DATABASE_URL=postgresql://chatapp_user:password@localhost:5432/chatapp
# JWT_SECRET=your_jwt_secret_here
# PORT=5000
# NODE_ENV=development
